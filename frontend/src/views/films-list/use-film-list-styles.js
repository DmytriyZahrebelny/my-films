import { makeStyles } from '@material-ui/core';

export const useFilmStylesStyles = makeStyles({
  root: {
    marginTop: 80,
  },
  warningMessage: {
    marginTop: 80,
    textAlign: 'center',
  },
});
