import { makeStyles } from '@material-ui/core';

export const useSearchFieldStyles = makeStyles({
  searchField: {
    display: 'block',
    width: 300,
    height: 30,
    padding: '0 10px',
    borderRadius: 20,
    border: 'none',
    outline: 'none',
  },
});
